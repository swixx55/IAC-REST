package test;

import util.BMIUtil;
import org.junit.Test;

import static org.junit.Assert.assertEquals;


public class BMITest {
    /*double DELTA = 1e-15;
    @Test
    public void geldigeInput() {
    BMIUtil w = new BMIUtil();
    double result = w.calculateBMIFor(10, 12);
    assertEquals(120, result, DELTA);
    }*/
}
